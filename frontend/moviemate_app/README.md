# MovieMate Flutter App

See the main project README at [`../../README.md`](../../README.md) for full setup instructions.

## Quick Start

```bash
flutter pub get
flutter run
```

Ensure the backend is running at `http://localhost:3000` before using the app.
